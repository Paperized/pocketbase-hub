# Required — your base domain (instances will be at <subdomain>.<APP_DOMAIN>)
APP_DOMAIN=pocket-hub.domain.com

# Dashboard credentials (HTTP Basic Auth)
DASHBOARD_USER=admin
DASHBOARD_PASS=changeme

# PostgreSQL — credentials for the bundled Postgres 17 container
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_please
